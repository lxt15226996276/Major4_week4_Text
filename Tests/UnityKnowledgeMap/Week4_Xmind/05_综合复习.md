# 综合案例综合复习

> 参照：[Unity 官方 Manual - Scripting](https://docs.unity3d.com/Manual/ScriptingSection.html) · [Unity 官方 Manual - ScriptableObjects](https://docs.unity3d.com/Manual/class-ScriptableObject.html)  
> 关联文档：[01_RPG世界（一）.md](./01_RPG世界（一）项目初始化与玩家基础.md) · [02_RPG世界（二）.md](./02_RPG世界（二）战斗系统与交互.md) · [03_RPG世界（三）.md](./03_RPG世界（三）任务与存档.md) · [04_RPG世界（四）.md](./04_RPG世界（四）AI与优化.md)  
> 学习路径：**知识回顾 → 实战演练 → 进阶挑战**，采用 **总—分—总** 结构。  
> 讲解维度：**核心知识回顾 → 案例实战 → 常见问题 → 进阶挑战**。  

---

## 【全文总述】

**RPG（角色扮演游戏）世界综合案例** 复习篇，对前四章内容进行全面梳理和实战演练。  
学习本案例，你将：
- 回顾项目初始化、玩家控制、动画系统、相机跟随
- 回顾战斗系统、交互系统、UI界面、DOTween动画
- 回顾任务系统、存档系统、背包系统、ScriptableObject
- 回顾怪物AI、状态机、性能优化、对象池
- 完成综合实战项目，整合所有知识

### 思维导图总览

```
综合案例综合复习
│
├── 第一章回顾：项目初始化与玩家基础
│   ├── 项目结构
│   ├── 角色控制器（CharacterController）
│   ├── 动画系统（Animator）
│   └── 相机跟随（CameraFollow）
│
├── 第二章回顾：战斗系统与交互
│   ├── 战斗系统（攻击、伤害、受击）
│   ├── 交互系统（Raycast、IInteractable）
│   ├── UI界面（血条、伤害数字）
│   └── DOTween动画
│
├── 第三章回顾：任务与存档
│   ├── 任务系统（ScriptableObject、QuestManager）
│   ├── 存档系统（JsonUtility、PlayerPrefs）
│   ├── 背包系统（Item、Inventory）
│   └── ScriptableObject数据驱动
│
├── 第四章回顾：AI与优化
│   ├── 怪物AI（NavMesh、EnemyAI）
│   ├── 状态机（FSM、BehaviorTree）
│   ├── 性能优化（对象池、LOD）
│   └── 高级技巧（泛型状态机、行为树）
│
├── 实战演练：完整RPG项目整合
│   ├── 项目结构搭建
│   ├── 玩家系统整合
│   ├── 战斗系统整合
│   ├── 任务系统整合
│   └── AI系统整合
│
└── 进阶挑战：扩展功能实现
    ├── 多人联机
    ├── 技能系统
    ├── Boss战
    └── 编辑器工具
```

| 学习阶段 | 学习目标 |
|----------|----------|
| **知识回顾** | 掌握前四章核心知识，能够独立完成每个模块 |
| **实战演练** | 整合所有知识，完成完整RPG项目 |
| **进阶挑战** | 扩展功能，提升项目复杂度 |

---

# 第一部分：知识回顾

## 第一章：项目初始化与玩家基础

### 1.1 核心知识速览

| 模块 | 核心知识点 |
|------|------------|
| **项目结构** | Scenes、Scripts、Prefabs、Resources、Materials、Models、Animations、Textures |
| **CharacterController** | Move()、SimpleMove()、isGrounded、velocity |
| **Animator** | Animator Controller、参数（Float/Bool/Trigger）、状态机、过渡 |
| **CameraFollow** | LateUpdate()、Vector3.Lerp()、Transform.LookAt() |

### 1.2 关键代码回顾

```csharp
// CharacterController移动
controller.Move(move * speed * Time.deltaTime);

// Animator参数设置
animator.SetFloat("Speed", speed);
animator.SetBool("IsRunning", isRunning);

// 相机跟随
Vector3 desiredPos = target.position + offset;
transform.position = Vector3.Lerp(transform.position, desiredPos, smoothSpeed * Time.deltaTime);
transform.LookAt(target);
```

### 1.3 常见问题

| 问题 | 解决方案 |
|------|----------|
| 角色穿模 | 调整CharacterController的Radius和Height |
| 动画不播放 | 检查Animator Controller参数是否正确 |
| 相机抖动 | 使用Lerp平滑过渡，降低smoothSpeed |
| 角色漂浮 | 确保isGrounded检测正确 |

---

## 第二章：战斗系统与交互

### 2.1 核心知识速览

| 模块 | 核心知识点 |
|------|------------|
| **战斗系统** | Raycast检测、攻击动画事件、伤害计算、受击反馈 |
| **交互系统** | Physics.Raycast()、LayerMask、IInteractable接口 |
| **UI界面** | Canvas、Image、Text、Slider、事件系统 |
| **DOTween** | DOMove()、DOFade()、DOScale()、Sequence |

### 2.2 关键代码回顾

```csharp
// 攻击检测
Ray ray = new Ray(transform.position + Vector3.up, transform.forward);
if (Physics.Raycast(ray, out RaycastHit hit, attackRange, enemyLayer))
{
    EnemyHealth enemy = hit.collider.GetComponent<EnemyHealth>();
    if (enemy != null) enemy.TakeDamage(attackDamage);
}

// 交互检测
Ray ray = Camera.main.ScreenPointToRay(Input.mousePosition);
if (Physics.Raycast(ray, out RaycastHit hit, interactDistance, interactLayer))
{
    IInteractable interactable = hit.collider.GetComponent<IInteractable>();
    if (interactable != null) interactable.Interact();
}

// DOTween动画
transform.DOMove(targetPos, 1f);
image.DOFade(0f, 1f);
```

### 2.3 常见问题

| 问题 | 解决方案 |
|------|----------|
| 攻击检测不到 | 检查LayerMask和攻击范围 |
| 交互没有反应 | 检查IInteractable接口实现 |
| UI不显示 | 检查Canvas渲染模式和层级 |
| DOTween报错 | 确保已导入DOTween包 |

---

## 第三章：任务与存档

### 3.1 核心知识速览

| 模块 | 核心知识点 |
|------|------------|
| **任务系统** | ScriptableObject、Quest状态（NotAccepted/InProgress/Completed/Claimed）、任务管理器 |
| **存档系统** | JsonUtility、PlayerPrefs、Application.persistentDataPath、加密保存 |
| **背包系统** | ScriptableObject物品定义、背包管理、UI显示、对象池 |
| **ScriptableObject** | CreateAssetMenu、数据驱动、编辑器编辑 |

### 3.2 关键代码回顾

```csharp
// ScriptableObject定义任务
[CreateAssetMenu(fileName = "NewQuest", menuName = "RPG/Quest")]
public class Quest : ScriptableObject
{
    public string questID;
    public string questName;
    public QuestState state;
}

// 存档
string json = JsonUtility.ToJson(saveData, true);
File.WriteAllText(Application.persistentDataPath + "/save.json", json);

// 读档
string json = File.ReadAllText(Application.persistentDataPath + "/save.json");
SaveData data = JsonUtility.FromJson<SaveData>(json);
```

### 3.3 常见问题

| 问题 | 解决方案 |
|------|----------|
| ScriptableObject不显示菜单 | 检查CreateAssetMenu参数 |
| Json序列化失败 | 确保类标记为[System.Serializable] |
| 文件找不到 | 使用Application.persistentDataPath |
| 背包UI卡顿 | 使用对象池优化 |

---

## 第四章：AI与优化

### 4.1 核心知识速览

| 模块 | 核心知识点 |
|------|------------|
| **怪物AI** | NavMeshAgent、SetDestination()、距离检测、状态切换 |
| **状态机** | FSM（有限状态机）、状态基类、状态转换、行为树 |
| **性能优化** | 对象池、LOD Group、批处理、遮挡剔除 |
| **高级技巧** | 泛型状态机、转换表、Selector/Sequence |

### 4.2 关键代码回顾

```csharp
// NavMesh寻路
navMeshAgent.SetDestination(player.position);

// 状态机基类
public abstract class State
{
    public abstract void Enter();
    public abstract void Update();
    public abstract void Exit();
}

// 对象池
public GameObject Get(Vector3 position, Quaternion rotation)
{
    GameObject obj = pool.Count > 0 ? pool.Dequeue() : Instantiate(prefab);
    obj.transform.position = position;
    obj.SetActive(true);
    return obj;
}
```

### 4.3 常见问题

| 问题 | 解决方案 |
|------|----------|
| AI不动 | 检查NavMesh是否烘焙、Area是否设置为Walkable |
| 状态机报错 | 检查空引用和状态转换条件 |
| 性能差 | 使用对象池、LOD、遮挡剔除 |
| AI穿墙 | 调整NavMeshAgent的Radius和Height |

---

# 第二部分：实战演练

## 一、完整RPG项目整合

### 1.1 项目结构搭建

```
Assets/
├── Scenes/
│   ├── MainMenu.unity
│   └── GameScene.unity
├── Scripts/
│   ├── Player/
│   │   ├── PlayerController.cs
│   │   ├── PlayerHealth.cs
│   │   └── PlayerAttack.cs
│   ├── Enemy/
│   │   ├── EnemyAI.cs
│   │   ├── EnemyHealth.cs
│   │   └── States/
│   │       ├── IdleState.cs
│   │       ├── ChaseState.cs
│   │       └── AttackState.cs
│   ├── UI/
│   │   ├── UIManager.cs
│   │   ├── HealthBar.cs
│   │   └── DamageText.cs
│   ├── Quest/
│   │   ├── Quest.cs
│   │   ├── QuestManager.cs
│   │   └── QuestUI.cs
│   ├── Inventory/
│   │   ├── Item.cs
│   │   ├── Inventory.cs
│   │   └── InventoryUI.cs
│   ├── Save/
│   │   ├── SaveManager.cs
│   │   └── SaveData.cs
│   └── Utility/
│       ├── ObjectPool.cs
│       └── EventManager.cs
├── Prefabs/
│   ├── Player.prefab
│   ├── Enemy.prefab
│   └── UI/
├── ScriptableObjects/
│   ├── Quests/
│   └── Items/
├── Animations/
│   ├── Player/
│   └── Enemy/
├── Materials/
└── Models/
```

### 1.2 玩家系统整合

```csharp
using UnityEngine;

public class PlayerController : MonoBehaviour
{
    CharacterController controller;
    Animator animator;
    PlayerHealth health;

    public float walkSpeed = 5f;
    public float runSpeed = 8f;
    public float jumpHeight = 2f;
    public float gravity = 9.8f;

    float currentSpeed;
    Vector3 velocity;
    bool isRunning;

    void Awake()
    {
        controller = GetComponent<CharacterController>();
        animator = GetComponent<Animator>();
        health = GetComponent<PlayerHealth>();
    }

    void Update()
    {
        if (health.isDead) return;

        float h = Input.GetAxisRaw("Horizontal");
        float v = Input.GetAxisRaw("Vertical");
        Vector3 move = new Vector3(h, 0, v).normalized;

        isRunning = Input.GetKey(KeyCode.LeftShift);
        currentSpeed = isRunning ? runSpeed : walkSpeed;

        if (move.magnitude >= 0.1f)
        {
            transform.rotation = Quaternion.LookRotation(move);
            controller.Move(move * currentSpeed * Time.deltaTime);
        }

        if (controller.isGrounded && velocity.y < 0) velocity.y = -2f;
        if (Input.GetKeyDown(KeyCode.Space) && controller.isGrounded)
            velocity.y = Mathf.Sqrt(jumpHeight * 2f * gravity);

        velocity.y -= gravity * Time.deltaTime;
        controller.Move(velocity * Time.deltaTime);

        animator.SetFloat("Speed", currentSpeed * move.magnitude);
        animator.SetBool("IsRunning", isRunning);
        animator.SetBool("IsGrounded", controller.isGrounded);
    }
}

public class PlayerHealth : MonoBehaviour
{
    public int maxHealth = 100;
    public int currentHealth;
    public bool isDead;
    HealthBar healthBar;

    void Awake()
    {
        currentHealth = maxHealth;
        healthBar = FindObjectOfType<HealthBar>();
        healthBar.SetMaxHealth(maxHealth);
    }

    public void TakeDamage(int damage)
    {
        currentHealth -= damage;
        healthBar.SetHealth(currentHealth);
        animator.SetTrigger("Hurt");

        if (currentHealth <= 0 && !isDead)
        {
            Die();
        }
    }

    void Die()
    {
        isDead = true;
        animator.SetTrigger("Die");
        GetComponent<PlayerController>().enabled = false;
    }
}
```

### 1.3 战斗系统整合

```csharp
using UnityEngine;

public class PlayerAttack : MonoBehaviour
{
    Animator animator;
    public float attackRange = 3f;
    public int attackDamage = 20;
    public LayerMask enemyLayer;
    bool isAttacking;

    void Awake()
    {
        animator = GetComponent<Animator>();
    }

    void Update()
    {
        if (Input.GetMouseButtonDown(0) && !isAttacking)
        {
            Attack();
        }
    }

    void Attack()
    {
        isAttacking = true;
        animator.SetTrigger("Attack");
    }

    public void OnAttackHit()
    {
        Ray ray = new Ray(transform.position + Vector3.up, transform.forward);
        if (Physics.Raycast(ray, out RaycastHit hit, attackRange, enemyLayer))
        {
            EnemyHealth enemy = hit.collider.GetComponent<EnemyHealth>();
            if (enemy != null)
            {
                enemy.TakeDamage(attackDamage);
                ShowDamageText(hit.point, attackDamage);
            }
        }
        isAttacking = false;
    }

    void ShowDamageText(Vector3 position, int damage)
    {
        GameObject damageText = ObjectPool.Instance.Get(position, Quaternion.identity);
        damageText.GetComponent<DamageText>().SetText(damage.ToString());
    }
}

public class EnemyHealth : MonoBehaviour
{
    public int maxHealth = 50;
    public int currentHealth;
    Animator animator;

    void Awake()
    {
        currentHealth = maxHealth;
        animator = GetComponent<Animator>();
    }

    public void TakeDamage(int damage)
    {
        currentHealth -= damage;
        animator.SetTrigger("Hurt");

        if (currentHealth <= 0)
        {
            Die();
        }
    }

    void Die()
    {
        animator.SetTrigger("Die");
        GetComponent<EnemyAI>().enabled = false;
        GetComponent<NavMeshAgent>().enabled = false;
        Destroy(gameObject, 3f);
    }
}
```

### 1.4 任务系统整合

```csharp
using UnityEngine;
using System.Collections.Generic;

[CreateAssetMenu(fileName = "NewQuest", menuName = "RPG/Quest")]
public class Quest : ScriptableObject
{
    public string questID;
    public string questName;
    public string description;
    public QuestType questType;
    public int targetCount;
    public int currentCount;
    public QuestState state;
    public Reward reward;
}

public enum QuestType { Kill, Collect, Talk, Explore }
public enum QuestState { NotAccepted, InProgress, Completed, Claimed }

[System.Serializable]
public class Reward
{
    public int gold;
    public int experience;
    public Item item;
}

public class QuestManager : MonoBehaviour
{
    public static QuestManager Instance;

    List<Quest> activeQuests = new List<Quest>();

    void Awake()
    {
        Instance = this;
    }

    public void AcceptQuest(Quest quest)
    {
        if (!activeQuests.Contains(quest))
        {
            quest.state = QuestState.InProgress;
            quest.currentCount = 0;
            activeQuests.Add(quest);
            QuestUI.Instance.AddQuest(quest);
        }
    }

    public void UpdateQuestProgress(string questID, int amount = 1)
    {
        Quest quest = activeQuests.Find(q => q.questID == questID);
        if (quest != null && quest.state == QuestState.InProgress)
        {
            quest.currentCount += amount;
            QuestUI.Instance.UpdateQuestUI(quest);

            if (quest.currentCount >= quest.targetCount)
            {
                quest.state = QuestState.Completed;
                QuestUI.Instance.UpdateQuestUI(quest);
            }
        }
    }

    public void ClaimReward(Quest quest)
    {
        if (quest.state == QuestState.Completed)
        {
            Inventory.Instance.AddItem(quest.reward.item);
            quest.state = QuestState.Claimed;
            QuestUI.Instance.RemoveQuest(quest);
        }
    }
}
```

### 1.5 AI系统整合

```csharp
using UnityEngine;
using UnityEngine.AI;

public enum EnemyState { Idle, Wander, Chase, Attack, Hurt, Dead }

public class EnemyAI : MonoBehaviour
{
    NavMeshAgent agent;
    Animator animator;
    Transform player;

    public EnemyState currentState;
    public float chaseRange = 10f;
    public float attackRange = 2f;
    public float wanderRadius = 5f;

    Vector3 wanderTarget;

    void Awake()
    {
        agent = GetComponent<NavMeshAgent>();
        animator = GetComponent<Animator>();
        player = GameObject.FindGameObjectWithTag("Player").transform;
        SetNewWanderTarget();
    }

    void Update()
    {
        if (currentState == EnemyState.Dead) return;

        float distance = Vector3.Distance(transform.position, player.position);

        switch (currentState)
        {
            case EnemyState.Idle:
                HandleIdle(distance);
                break;
            case EnemyState.Wander:
                HandleWander(distance);
                break;
            case EnemyState.Chase:
                HandleChase(distance);
                break;
            case EnemyState.Attack:
                HandleAttack(distance);
                break;
        }
    }

    void HandleIdle(float distance)
    {
        if (distance < chaseRange) ChangeState(EnemyState.Chase);
    }

    void HandleWander(float distance)
    {
        if (distance < chaseRange) { ChangeState(EnemyState.Chase); return; }

        if (Vector3.Distance(transform.position, wanderTarget) < 1f)
            SetNewWanderTarget();

        agent.SetDestination(wanderTarget);
        animator.SetFloat("Speed", agent.velocity.magnitude);
    }

    void HandleChase(float distance)
    {
        if (distance > chaseRange) { ChangeState(EnemyState.Idle); return; }
        if (distance < attackRange) { ChangeState(EnemyState.Attack); return; }

        agent.SetDestination(player.position);
        animator.SetFloat("Speed", agent.velocity.magnitude);
    }

    void HandleAttack(float distance)
    {
        if (distance > attackRange) { ChangeState(EnemyState.Chase); return; }

        agent.isStopped = true;
        transform.LookAt(player);
        animator.SetTrigger("Attack");
    }

    void SetNewWanderTarget()
    {
        Vector3 randomPos = Random.insideUnitSphere * wanderRadius;
        randomPos += transform.position;
        NavMeshHit hit;
        NavMesh.SamplePosition(randomPos, out hit, wanderRadius, NavMesh.AllAreas);
        wanderTarget = hit.position;
    }

    void ChangeState(EnemyState newState)
    {
        currentState = newState;
        agent.isStopped = false;
    }

    public void TakeDamage(int damage)
    {
        if (currentState == EnemyState.Dead) return;
        currentState = EnemyState.Hurt;
        animator.SetTrigger("Hurt");
        Invoke("ResumeState", 0.5f);
    }

    void ResumeState()
    {
        if (currentState != EnemyState.Dead) currentState = EnemyState.Idle;
    }

    public void Die()
    {
        currentState = EnemyState.Dead;
        agent.isStopped = true;
        animator.SetTrigger("Die");
        QuestManager.Instance.UpdateQuestProgress("kill_enemy");
    }
}
```

---

# 第三部分：进阶挑战

## 一、扩展功能实现

### 1.1 技能系统

```csharp
using UnityEngine;

[CreateAssetMenu(fileName = "NewSkill", menuName = "RPG/Skill")]
public class Skill : ScriptableObject
{
    public string skillName;
    public Sprite icon;
    public float cooldown;
    public float damage;
    public float range;
    public SkillType skillType;

    float currentCooldown;

    public bool CanUse()
    {
        return currentCooldown <= 0;
    }

    public void Use(Transform caster)
    {
        currentCooldown = cooldown;
        switch (skillType)
        {
            case SkillType.Fireball:
                CastFireball(caster);
                break;
            case SkillType.Heal:
                Heal(caster);
                break;
        }
    }

    void CastFireball(Transform caster)
    {
        GameObject fireball = ObjectPool.Instance.Get(caster.position + caster.forward, caster.rotation);
        fireball.GetComponent<Fireball>().Initialize(damage, range);
    }

    void Heal(Transform caster)
    {
        PlayerHealth health = caster.GetComponent<PlayerHealth>();
        if (health != null) health.TakeDamage(-damage);
    }

    public void UpdateCooldown()
    {
        if (currentCooldown > 0) currentCooldown -= Time.deltaTime;
    }
}

public enum SkillType { Fireball, Heal, IceSpear, Lightning }

public class Fireball : MonoBehaviour
{
    float damage;
    float range;
    Vector3 startPos;

    public void Initialize(float damage, float range)
    {
        this.damage = damage;
        this.range = range;
        startPos = transform.position;
    }

    void Update()
    {
        transform.Translate(Vector3.forward * 10f * Time.deltaTime);

        if (Vector3.Distance(startPos, transform.position) > range)
        {
            ObjectPool.Instance.Return(gameObject);
        }
    }

    void OnCollisionEnter(Collision collision)
    {
        EnemyHealth enemy = collision.collider.GetComponent<EnemyHealth>();
        if (enemy != null) enemy.TakeDamage((int)damage);
        ObjectPool.Instance.Return(gameObject);
    }
}
```

### 1.2 Boss战系统

```csharp
using UnityEngine;

public class Boss : MonoBehaviour
{
    public int maxHealth = 500;
    public int currentHealth;
    public float attackRange = 3f;
    public float chaseRange = 15f;
    public float attackInterval = 2f;

    NavMeshAgent agent;
    Animator animator;
    Transform player;

    float attackTimer;
    int phase;

    void Awake()
    {
        currentHealth = maxHealth;
        agent = GetComponent<NavMeshAgent>();
        animator = GetComponent<Animator>();
        player = GameObject.FindGameObjectWithTag("Player").transform;
        phase = 1;
    }

    void Update()
    {
        float distance = Vector3.Distance(transform.position, player.position);

        if (distance < attackRange)
        {
            Attack();
        }
        else if (distance < chaseRange)
        {
            Chase();
        }

        attackTimer += Time.deltaTime;
        CheckPhase();
    }

    void Attack()
    {
        if (attackTimer >= attackInterval)
        {
            attackTimer = 0;
            agent.isStopped = true;
            transform.LookAt(player);

            if (phase == 1)
                animator.SetTrigger("Attack1");
            else if (phase == 2)
                animator.SetTrigger("Attack2");
            else
                animator.SetTrigger("Attack3");
        }
    }

    void Chase()
    {
        agent.isStopped = false;
        agent.SetDestination(player.position);
        animator.SetFloat("Speed", agent.velocity.magnitude);
    }

    void CheckPhase()
    {
        float healthPercent = (float)currentHealth / maxHealth;
        if (healthPercent < 0.3f && phase != 3)
        {
            phase = 3;
            animator.SetTrigger("Phase3");
        }
        else if (healthPercent < 0.6f && phase != 2)
        {
            phase = 2;
            animator.SetTrigger("Phase2");
        }
    }

    public void TakeDamage(int damage)
    {
        currentHealth -= damage;
        animator.SetTrigger("Hurt");

        if (currentHealth <= 0)
        {
            Die();
        }
    }

    void Die()
    {
        animator.SetTrigger("Die");
        agent.isStopped = true;
        Destroy(gameObject, 5f);
    }
}
```

### 1.3 编辑器工具

```csharp
using UnityEngine;
using UnityEditor;

public class QuestEditorWindow : EditorWindow
{
    Quest currentQuest;

    [MenuItem("Window/RPG/Quest Editor")]
    public static void ShowWindow()
    {
        GetWindow<QuestEditorWindow>("Quest Editor");
    }

    void OnGUI()
    {
        GUILayout.Label("Quest Editor", EditorStyles.boldLabel);

        currentQuest = EditorGUILayout.ObjectField("Quest", currentQuest, typeof(Quest), false) as Quest;

        if (currentQuest != null)
        {
            Undo.RecordObject(currentQuest, "Edit Quest");

            currentQuest.questName = EditorGUILayout.TextField("Quest Name", currentQuest.questName);
            currentQuest.description = EditorGUILayout.TextArea(currentQuest.description, GUILayout.Height(50));
            currentQuest.questType = (QuestType)EditorGUILayout.EnumPopup("Quest Type", currentQuest.questType);
            currentQuest.targetCount = EditorGUILayout.IntField("Target Count", currentQuest.targetCount);

            EditorGUILayout.LabelField("Reward");
            currentQuest.reward.gold = EditorGUILayout.IntField("Gold", currentQuest.reward.gold);
            currentQuest.reward.experience = EditorGUILayout.IntField("Experience", currentQuest.reward.experience);
            currentQuest.reward.item = EditorGUILayout.ObjectField("Item", currentQuest.reward.item, typeof(Item), false) as Item;

            if (GUI.changed)
            {
                EditorUtility.SetDirty(currentQuest);
            }
        }
    }
}
```

---

# 【全文总结】

## 核心知识框架

| 章节 | 核心模块 | 关键技术 |
|------|----------|----------|
| 第一章 | 玩家控制 | CharacterController、Animator、CameraFollow |
| 第二章 | 战斗交互 | Raycast、IInteractable、DOTween |
| 第三章 | 任务存档 | ScriptableObject、JsonUtility、PlayerPrefs |
| 第四章 | AI优化 | NavMesh、FSM、ObjectPool |

## 最重要的代码

```csharp
// 玩家移动
controller.Move(move * speed * Time.deltaTime);

// 攻击检测
Physics.Raycast(ray, out hit, range, layer);

// 任务系统
quest.state = QuestState.InProgress;

// AI寻路
navMeshAgent.SetDestination(player.position);

// 存档
JsonUtility.ToJson(saveData, true);
```

## 学习自检

### 基础篇
- [ ] 能搭建完整项目结构
- [ ] 能实现玩家移动和动画
- [ ] 能实现相机跟随
- [ ] 能实现基本战斗系统

### 进阶篇
- [ ] 能实现任务系统
- [ ] 能实现存档系统
- [ ] 能实现背包系统
- [ ] 能实现怪物AI

### 高级篇
- [ ] 能实现状态机
- [ ] 能实现对象池
- [ ] 能实现技能系统
- [ ] 能实现编辑器工具

## 常见错误速查

| 错误 | 原因 | 解决 |
|------|------|------|
| NullReferenceException | 组件未获取 | 在Awake/Start中获取组件 |
| 动画不播放 | 参数不匹配 | 检查Animator参数名 |
| AI不动 | NavMesh未烘焙 | 烘焙导航网格 |
| 存档失败 | 类未标记Serializable | 添加[System.Serializable] |
| UI不显示 | Canvas层级问题 | 调整Canvas渲染模式 |

---

## 参考资料

| 类型 | 链接 |
|------|------|
| CharacterController | https://docs.unity3d.com/ScriptReference/CharacterController.html |
| Animator | https://docs.unity3d.com/ScriptReference/Animator.html |
| NavMeshAgent | https://docs.unity3d.com/ScriptReference/AI.NavMeshAgent.html |
| JsonUtility | https://docs.unity3d.com/ScriptReference/JsonUtility.html |

---

*文档版本：与 major3 系列、Week2_Xmind、Week3_Xmind、Week4_Xmind 同系列模板。*